# Optional comma-separated additional HTTPS origins allowed to submit the contact form.
# The private Sites domain is already allowed. Add the verified public domain at launch.
CONTACT_ALLOWED_ORIGINS=
# Delivery uses FormSubmit to info@goldkrest.group; confirm the activation email once.
